export interface Restaurant {
    restaurant: string;
    distance:number;
    completed: boolean;
  }

  export interface Amenties{
    amenti:string;
    description:string;
    list:boolean;
  }

  export interface Surroundings{
    name:string;
    description:string;
    distance:number;
    list:boolean;
  }