import { AdminDistencePipe } from './admin-distence.pipe';

describe('AdminDistencePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminDistencePipe();
    expect(pipe).toBeTruthy();
  });
});
